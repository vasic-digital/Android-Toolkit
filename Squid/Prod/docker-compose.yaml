version: "3"
services:
  proxy:
    image: ubuntu/squid
     ports:
       - "0.0.0.0:${PROXY_LOCAL_PROD_PORT}:3128"
    volumes:
      - ./squid.conf:/etc/squid/squid.conf
      - ./passwords:/etc/squid/passwords
    restart: always