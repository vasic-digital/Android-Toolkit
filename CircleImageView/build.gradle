apply plugin: 'com.android.library'

android {

    compileSdk 36
    namespace 'com.redelf.widget.circleimageview'

    defaultConfig {

        minSdk 28
        targetSdk 36

        multiDexEnabled true
    }

    sourceSets {

        main {

            java.srcDirs += "build/generated/ksp"
        }
    }
}

dependencies {

    compileOnly 'androidx.annotation:annotation:1.9.1'
}
